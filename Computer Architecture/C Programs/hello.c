#include <stdio.h>

int main(void){
    // int numbers[] = {1, 2, 3, 4};
    // *(numbers + 3) = 2;
    // int *aPointer = numbers;
    // int *bPointer = &(numbers[0]);


    // printf("%d\n", *(numbers)); 
    // printf("%d\n", *(aPointer)); 
    // printf("%d\n", *(bPointer)); 
    //
    // printf("%zu\n", sizeof(*(numbers))); 
    // printf("%zu\n", sizeof(*(numbers + 2))); 
    //
    // printf("%p\n", &(*(numbers))); 
    // printf("%p\n", &(*(numbers + 2))); 
    //
    // printf("%p\n", (void *)&(*(numbers))); 
    // printf("%p\n", (void *)&(*(numbers + 2))); 
    

    int numbers[] = {1, 2, 3, 4};
    printf("%d\n", *(numbers)); 
    return 0;
}
